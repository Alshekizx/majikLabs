/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // You can add your own settings here
    // Add other configurations as necessary
  };
  
  export default nextConfig;